{
  "name": "dsh-win-toast-notify",
  "version": "1.3.1",
  "private": true,
  "description": "DeepSeek Harness (DSH) dynamic Cordis plugin: Windows native toast notification when an agent task completes (WSL2).",
  "keywords": [
    "dsh",
    "dsh-plugin",
    "deepseek-harness",
    "cordis",
    "plugin",
    "wsl2",
    "windows",
    "toast",
    "notification"
  ],
  "license": "MIT",
  "files": [
    "host.js",
    "win-task-notify.mjs",
    "scripts"
  ],
  "scripts": {
    "test": "node --test test/plugin.test.mjs",
    "check": "node --check host.js && node --check win-task-notify.mjs && bash -n scripts/test-toast.sh"
  }
}
